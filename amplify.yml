version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "No build required - Node.js server app"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
    excludePaths:
      - node_modules/**/*
      - .git/**/*
      - .env
