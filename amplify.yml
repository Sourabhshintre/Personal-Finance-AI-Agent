version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
        - echo "Backend build completed"
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "Preparing frontend files"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
