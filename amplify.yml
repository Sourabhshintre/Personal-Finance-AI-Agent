version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "No build step required for this app"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
